#!/bin/bash
# Сверху шебанг - определяет программу-интерпретатор, которая будет
# исполнять этот скрипт.  

name="Joe"
# Сравниванием одну строку с другой строкой
# Переменные дожны начинаться с $.
# if [[условие]]; then 
#     ... 
# fi
# На самом деле, можно и писать if [условие]
# [...] - синтаксический сахар для вызова команды test,
# [[...]] - ключевое слово bash.
# Обе конструкции возвращают код возврата, по которому определяется
# выполнилось условие или нет.

if [[ "$name" = "Joe" ]]; then
    # После then блок кода который нужно выполнить, если условие выполнилось. 
    echo "What's up, Joe?"
# fi для завершения блока if
fi

name="Jack"
# Используем else для выполнения блока кода, если условие не выполнилось. 
if [[ "$name" = "Joe" ]]; then
    echo "What's up, Joe?"
else
    echo "Hello, $name!"
fi

# Сравниваем числа
# -eq - равно
# -ne - не равно
# -gt - больше
# -ge - больше или равно
# -lt - меньше
# -le - меньше или равно
if [[ 42 -gt 7 ]]; then
    echo "42 is the answer to life, the universe, and everything."
fi

# Используем elif для проверки нескольких условий
num=123
if [[ $num -ne 123 ]]; then
    echo "You've just encountered a cosmic ray and your number has changed!"
# (здесь можно использовать и else)
elif [[ $num -eq 123 ]]; then
    echo "Your number is still 123."
fi
